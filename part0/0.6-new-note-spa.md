# Exercise 0.6 - New Note in SPA

```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: the Javascript handles the form submit, adds new note to the local list, and rerenders it locally

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: HTTP 201 Created (JSON object confirming creation) 
    deactivate server

```




