# Exercise 0.6 - New Note in SPA

```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: Javascript handles the form submission and prevents the default page reload

    Note right of browser: Javascript adds the new note to the local list and rerenders the note

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: HTTP 201 Created (JSON object confirming creation) 
    deactivate server

```




